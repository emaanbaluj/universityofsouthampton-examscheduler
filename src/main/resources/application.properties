spring.application.name=UniversityOfSouthamptonExamScheduler
# Check if the server.port or server.servlet.context-path is set correctly
server.port=8080
server.servlet.context-path=/api
# PostgreSQL connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/uosexams
spring.datasource.username=postgres
spring.datasource.password=Veedaisbrainless12!

# JPA settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true  
spring.jpa.properties.hibernate.format_sql=true